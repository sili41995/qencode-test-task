import styled from '@emotion/styled';

export const StyledSection = styled.section`
  padding-top: ${({ theme }) => theme.padding.mainSection}px;
  padding-bottom: ${({ theme }) => theme.padding.mainSection}px;
`;
