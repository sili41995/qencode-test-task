import AuthSection from '@/components/AuthSection';
import { FC } from 'react';

const LoginPage: FC = () => {
  return (
    <AuthSection>
      <div>LoginPage</div>
    </AuthSection>
  );
};

export default LoginPage;
